services:
  - type: web
    name: universal-dataset-to-excel
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --threads 4 --timeout 1800 --bind 0.0.0.0:$PORT
